dbt_transform:
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      user: postgres
      password: mysecretpassword
      dbname: sales
      schema: dbt
      threads: 4
      keepalives_idle: 0
  target: dev